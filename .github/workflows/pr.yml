name: PR

on:
  pull_request:
    branches:
      - main
  push:
    branches-ignore:
      - main

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

env:
  XCODE_VERSION: "latest-stable"
  # The swift-makefile bootstrap fetches swift.mk via `gh api` on every make
  # invocation; without a token gh fails and the fetch silently writes an empty
  # swift.mk, so the build target never loads. Authenticate the public fetch.
  GH_TOKEN: ${{ github.token }}

jobs:
  verify:
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup-build-env
        with:
          xcode-version: ${{ env.XCODE_VERSION }}
          github-token: ${{ github.token }}

      - name: Install Tuist dependencies
        run: make install-dependencies

      - name: Verify project
        run: make verify CODE_SIGN_IDENTITY="-"

      - name: Lint project
        run: make lint CODE_SIGN_IDENTITY="-"

      - name: Build project
        run: make build CODE_SIGN_IDENTITY="-"
